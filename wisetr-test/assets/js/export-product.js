var siteurl = CALL.siteurl;//siteurl
var ajaxurl = CALL.ajaxurl;//ajaxurl

jQuery(document).ready(function($){
	
	jQuery('#woo_product_export').select2({
	    placeholder: "Select Fields",
	    allowClear: true
	});
});
